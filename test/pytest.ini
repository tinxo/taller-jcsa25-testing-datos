[pytest]
testpaths =
    test
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore:result_format configured at the Validator-level will not be persisted.:UserWarning:great_expectations.expectations.expectation
